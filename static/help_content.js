"use strict";
/* Structured help kept separate from application code so documentation can
   evolve without making the controller harder to maintain. */
window.HELP_SECTIONS=[
 {id:"start",title:"Getting Started",body:`
  <p>u64deck runs locally in the browser and controls one selected Ultimate at a time. Use <b>Select Ultimate…</b>, choose a verified address, then work from SCREEN, STORAGE, ASSEMBLY64 or SID JUKEBOX.</p>
  <h3>Basic workflow</h3><span class="help-example">1. Select the Ultimate.
2. Open STORAGE and browse /USB0, /SD, /Flash or /Temp.
3. Mount, run or inspect software.
4. Return to SCREEN and click the C64 display before typing.</span>
  <p>The interface can also be opened from another device on the LAN using this PC's address and port 8064 when u64deck listens on <code>0.0.0.0</code>. Keep the server running on the host PC; closing the browser does not stop it.</p>`},
 {id:"finder",title:"Find Ultimate Devices",body:`
  <p><b>Select Ultimate…</b> identifies physical Ultimate systems rather than treating every IP address as a separate device. Ethernet and Wi-Fi endpoints are verified using <code>/v1/info</code> and grouped using the firmware unique id. The friendly hostname is shown first; the numeric identity remains available for deduplication and troubleshooting.</p>
  <h3>Cached addresses first</h3><p>Previously verified addresses on the current local subnet are checked first so the normal case is fast. Remembered addresses are candidates only: an address must answer during this scan before it can be displayed, selected or recommended. Stale and off-subnet history is never promoted into the live result.</p>
  <h3>Bounded subnet scan</h3><p>u64deck places known addresses at the front of the same concurrent pass as the remaining local <code>/24</code>, so stale persisted history cannot delay the fresh scan. Each candidate receives one direct <code>GET /v1/info</code> request. The application and supplied diagnostic share the same bounded 64-worker <code>ThreadPoolExecutor + socket</code> transport. TCP connection establishment is limited to 1.5 seconds; only after a connection succeeds does the Ultimate receive a separate 3.25-second HTTP-response allowance. This accommodates firmware that accepts an Ethernet connection quickly but needs more than two seconds to answer, without applying the longer delay to unused addresses. There is no preliminary port scan, async HTTP substitute or retry pass, and each address is requested once. Routine status and Mounted Drives polling pause as soon as Finder opens and remain paused while the scan is active.</p>
  <h3>One row per physical device</h3><p>When Ethernet and Wi-Fi respond with the same unique id, u64deck groups the two addresses into one row and recommends Ethernet. Link classification uses current MAC/ARP evidence only and sends no extra REST latency probes. Select the Ethernet or Wi-Fi button, confirm the visible highlight, then press <b>Use selected address</b>. The connected header shows the active link type and offers <b>Switch to Ethernet</b> only when the wired address has been verified.</p>
  <h3>Responsive hand-off</h3><p>Remembered addresses are included at the front of the same concurrent scan as the local /24, so stale history cannot delay fresh discovery. A verified Finder result is reused when connecting, so u64deck does not immediately repeat <code>/v1/info</code> or link-classification work. The optional CIA1 keyboard capability check does not block Connect. Status and Mounted Drives polling stand down while user-initiated device operations are active, preventing stale requests from piling up behind Connect, video or audio controls.</p>
  <h3>Link-aware features</h3><p>The active connection type affects network-dependent features. Screen video, audio and recording are wired-only, so they are clearly gated over Wi-Fi while Storage, SID, Settings, discovery and supported machine controls remain available. Unknown means the link could not be classified safely; it is not guessed as Ethernet or Wi-Fi.</p>
  <h3>DHCP and disabled interfaces</h3><p>The full subnet phase discovers replacement DHCP addresses. Matching interface MAC addresses supersede old entries. Disabled Wi-Fi or disconnected Ethernet disappears on the next scan because only current responses are displayed.</p>
  <h3>Clear discovered devices</h3><p>Use this recovery action when remembered identities or addresses are no longer useful. It clears discovery history, the active identity and current connection, preserves unrelated settings such as passwords, paths, mount modes and Assembly64 options, then starts a genuinely fresh scan.</p>`},
 {id:"screen",title:"Screen & Recording",body:`
  <p><b>Reset C64</b> restarts the C64 core. <b>Reboot Ultimate</b> restarts the complete device and briefly interrupts streams and network control. <b>Ultimate Menu</b> opens the on-device menu.</p>
  <p>Before video starts, after <b>Stop Video</b>, or after an unexpected disconnect, the canvas shows <b>VIDEO NOT CONNECTED</b> rather than retaining a frozen frame. If audio remains active, the panel says so.</p>
  <h3>Streaming</h3><p><b>Stream Frames</b> controls live WebSocket buffering. Display Style, Display Size, Stream Route and Network Interface affect viewing, not recording quality.</p>
  <p>The Ultimate firmware sends VIC video and audio only through its wired network interface. On a positively identified Wi-Fi connection, u64deck displays <b>STREAMING NOT AVAILABLE OVER WI-FI</b> and disables Start Video, Start Audio, Record and Full Screen. Storage, Settings, SID playback and all other REST features remain available, with a longer timeout for the slower Wi-Fi bridge. Use <b>Switch to Ethernet</b> when the wired address is known.</p>
  <p>For an <b>Unknown</b> link, stream controls remain enabled. If the start request succeeds but no video frame arrives after about five seconds, u64deck shows a non-modal hint suggesting Ethernet; this is guidance rather than a diagnosis because a firewall can produce the same symptom.</p>
  <h3>Keyboard</h3><p>Click the canvas to capture input. Esc sends RUN/STOP, Backspace sends DEL, Home sends HOME and Shift+Home sends CLR. CIA1 input supports F1–F8 and cursor keys. On Legacy KERNAL-buffer input, Retro Replay F7 must be pressed on the physical C64 keyboard; u64deck suppresses remote Legacy F7 because hardware testing showed that injected code can enter the cartridge Freeze Menu. Automatic fastload-menu handling is currently supported only for Retro Replay.</p><p>u64deck probes the selected Ultimate for CIA1 matrix input. When available, physical key-down and key-up events preserve held keys and chords, so cracktros, games and the Ultimate menu can be controlled remotely. SPACE, RUN/STOP and RESTORE quick keys appear beside the screen. Older firmware and unsupported hardware fall back automatically to the KERNAL keyboard buffer.</p><p>Legacy screen-mirror keys are time-sensitive. While Mount &amp; Run owns the device they are rejected rather than queued, and a manual Legacy key request that cannot reach the device within two seconds expires without being sent. Manual Reset/Reboot requests are likewise rejected during Mount &amp; Run, coalesced when duplicated and expired rather than delivered late. The CIA1 matrix path retains its existing <code>release_all</code> safety handling.</p><p><b>Auto F7 Fastload</b> is deliberately limited to the hardware-tested <b>Retro Replay</b> flow, including the firmware cartridge identifier <code>rr38pal</code>. u64deck reads the Ultimate firmware's current Cartridge setting before Mount &amp; Run. With no configured cartridge it follows the normal BASIC path with no F7 or prompt. With Retro Replay and CIA1, matrix F7 remains fully automatic when the saved option is enabled; when it is disabled, the prompt explains that startup is manual without mentioning a Legacy limitation. With Retro Replay and Legacy input, the saved preference is retained but automatic F7 is unavailable because an emulated F7 can open the Freeze Menu, so a compact persistent <b>Physical F7 required</b> card asks for the physical C64 key. Other configured cartridges are not rejected: u64deck sends no guessed F-key and uses mode-specific wording to ask the user to reach BASIC. Mount &amp; Run cards include <b>Cancel Mount &amp; Run</b>; firmware without the read-memory endpoint provides <b>Continue</b> after BASIC READY.</p><p><b>Legacy Retro Replay compatibility remains best-effort on the tested C64 Ultimate.</b> Allow approximately 3–5 seconds between Reset/Reboot actions and before starting Mount &amp; Run. Other freezer/fastload cartridges have not been comprehensively tested, and this behaviour was not observed on the CIA1 matrix-input path.</p><p>Standalone <b>Reset</b> and <b>Reboot</b> use a separate informational overlay only for Legacy + Retro Replay while the saved Auto F7 preference is enabled. It does not try to detect the physical keypress or hold the coordinator. Instead, it performs one short BASIC-readiness sample at a time, releases the coordinator after every sample and requires two consecutive ready readings. When Fastload/BASIC is detected, the card briefly reports <b>Fastload detected — ready</b> and clears automatically. Firmware without the read-memory endpoint retains the Dismiss-only behaviour. The card remains available when another tab is open, includes <b>Dismiss</b>, and is cleared by another Reset/Reboot, Mount &amp; Run, device change, CIA1 detection or a temporary CRT launch.</p><p>Mount &amp; Run waits for the KERNAL screen editor before typing <code>LOAD</code> and again before dispatching <code>RUN</code>. It requires two consecutive ready readings and records each gate result in Diagnostics. On CIA1-capable U64 firmware the complete LOAD line and later RUN line are sent as ordered matrix key taps. Legacy/C64U sessions retain KERNAL-buffer LOAD and RUN delivery, but no automatic F7 is injected. During a slow genuine-drive load the header shows <b>BUSY — loading program…</b> instead of treating the Ultimate's occupied HTTP service as offline. If the machine remains busy for 2 minutes, nothing further is typed and a status note appears.</p>
  <h3>Recording</h3><p>The recorder uses the browser's MediaRecorder support and needs no FFmpeg. Auto prefers MP4 where supported and otherwise uses WebM. The red recording dot pulses, but controls and tooltips remain steady.</p>
  <p>Chrome and Edge can optionally ask where to save. Other browsers use their normal Downloads folder. WebM recordings receive duration metadata before saving to improve seeking.</p>`},
 {id:"favourites",title:"Favourites",body:`
  <p>Use the star beside folders, disk images, programs, Assembly64 releases, SID tunes or folders, Quick Launch files and launchable files inside disk images.</p>
  <p>Recently Used updates automatically when content is launched. Removing a star changes only the Favourites list and never alters the underlying item.</p>
  <p>SID tune cards have both <b>Play</b> and <b>＋</b>. Play creates and starts a one-tune queue; ＋ appends the saved SID to the current play queue without interrupting playback.</p>
  <p>In the SID Jukebox, the star beside the playback controls favourites the exact tune currently playing. It mirrors the star in the Play Queue, so a tune discovered through Random Dive can be saved without scrolling to its row.</p>`},
 {id:"storage",title:"Storage",body:`
  <p><b>Browse Ultimate Storage</b> handles attached storage, programs, cartridges, SID files and disk images. <b>Files From This PC</b> handles a file selected from the local computer.</p>
  <h3>Mounted Drives</h3><p>The compact strip at the top of SCREEN and STORAGE shows Drive A and Drive B with the current image and effective mode; click it to jump to the full eject and refresh controls. After Mount &amp; Run, the strip updates as soon as the Ultimate confirms the mount, before reset and loading finish. During a slow genuine-drive load the confirmed filename remains visible with an amber loading note instead of being replaced by a timeout. When loading ends, u64deck immediately reads the real drive state and reconciles the display.</p>
  <h3>Default mount mode</h3><ul><li><b>Unlinked — temporary writes</b>: the safe default for existing images. Drive writes are discarded when the image is removed or replaced.</li><li><b>Read-only (RO)</b>: blocks drive writes.</li><li><b>Read/write (RW)</b>: saves drive writes to the original image.</li></ul>
  <p>Mount buttons show their effective mode directly, for example <b>Mount &amp; Run · UNLINKED</b>.</p>
  <h3>Create Blank Image</h3><p>A D64, D71, D81 or DNP created through u64deck is immediately mounted <b>Read/write</b> on drive A because the normal reason for creating a blank image is to populate it. When opened again later through Storage, Search or Favourites, it is treated as an existing image and uses the selected default mode.</p>
  <h3>Disk image contents</h3><p><b>Open Image</b> parses supported D64, D71 and D81 directories without altering the image. <b>Run Instantly</b> extracts and DMA-runs one PRG without a drive. <b>Load</b> transfers it without RUN. <b>Mount &amp; Load</b> keeps the parent image mounted and types the selected filename for multi-load software. Images with damaged, protected or non-standard directory chains can still be mounted even when their contents cannot be parsed.</p>
  <h3>Safer permanent writes</h3><p><b>Back Up &amp; Mount RW</b> creates a timestamped sibling copy before mounting the original read/write.</p>
  <h3>Automatic disk swap sets</h3><p>u64deck groups only closely matching sibling names such as <code>Game side1.d64</code> / <code>Game side2.d64</code>, <code>Game (Disk 1).d64</code> / <code>Game (Disk 2).d64</code>, <code>ThePhoenixCode-Disk1-BZ.D64</code> / <code>ThePhoenixCode-Disk2-BZ.D64</code>, or <code>Scratch-1.d64</code> / <code>Scratch-2.d64</code>. Shared release suffixes must also match. It also recognises compound numbering (<code>EdgeOfDisgrace_0.d64</code> / <code>_1a.d64</code> / <code>_1b.d64</code>), bare parenthesised tokens (<code>WeAreDemo(A).d64</code> / <code>(B).d64</code>, <code>Game(1).d64</code> / <code>(2).d64</code>) and title-less marker names (<code>side1.d64</code> / <code>side2.d64</code>) within one folder. It also recognises terminal hyphen-delimited pairs such as <code>the-hat-7a825b1-a.d64</code> / <code>the-hat-7a825b1-b.d64</code>. Square-bracket letters, bare tokens beside an unsuffixed sibling and glued sequel-like digits remain deliberately excluded. Ambiguous names stay a one-disk set; use Disk Swap Queue to choose a set manually.</p><p>After a mount, u64deck briefly reports the match and lists the related filenames. On startup and <b>Refresh Drive Status</b>, it reads the currently mounted device path and reconstructs the automatic family. Manual and uploaded queues are retained while their mounted member remains active.</p>`},
 {id:"quick-launch",title:"Quick Launch",body:`
  <p>QUICK LAUNCH on the SCREEN tab is a small local library for files you use repeatedly. Choose a supported CRT, PRG, T64, SID, MOD or disk image and select <b>Add to library</b>. u64deck stores the file in the local <code>library</code> folder beside the application and creates a one-click launch button; nothing is uploaded until that button is used.</p>
  <p>Programs, cartridges, SIDs and modules use the matching Ultimate runner. T64 files run their first PRG. Disk images are uploaded to the selected drive using the current Default Mount Mode and reset the C64. A CRT run directly from u64deck remains temporary runner state; before a later Mount &amp; Run, u64deck performs one full Ultimate reboot, restores the firmware-configured cartridge and then applies the normal cartridge preflight. Right-click a Quick Launch button to remove its local copy. The star beside a button adds or removes it from Favourites without changing the library file.</p>`},
 {id:"search",title:"Search Index",body:`
  <p>The SQLite catalogue stores storage entries, parsed D64/D71/D81 contents and SID metadata. Normal browsing refreshes individual folders. <b>Build Index</b> walks a complete subtree. After a successful local import, the same action becomes <b>Verify Index</b>.</p>
  <p>u64deck uses one stable <code>.u64deck-index.sqlite3</code> beside the application. It is not tied to the Ultimate's DHCP address. On the first launch after upgrading, u64deck detects older per-IP databases, creates transactionally consistent copies under a dated <code>index-backups</code> folder, merges the newest valid records from all of them, skips unusable orphaned cache rows left by historic or interrupted writes, validates foreign-key integrity and storage/image/SID coverage, and only then switches to the stable database. The originals are not modified. <b>Search Index &amp; Cache</b> shows the active filename and how many legacy databases were merged.</p>
  <p><b>Search Inside Disk Images</b> includes catalogued files inside supported image formats. Results can open a containing folder, mount or run an image, inspect its contents, or launch a catalogued PRG from inside it. Images whose directory cannot be parsed remain indexed as ordinary storage files and can still be mounted.</p>
  <p>The time-limit selector bounds live walks; indexed results remain available while a verification or background build is running. Pause leaves completed work in SQLite and Resume continues it. Stop ends the current job without deleting the existing catalogue.</p>
  <h3>Disk-image naming analysis</h3><p><b>Analyse Disk-Image Names</b> uses only D64, D71, D81 and G64 filenames already stored in SQLite. It reports recognised sets, high-confidence missed patterns, ambiguous names and protected/rejected conventions. <b>Copy Analysis Report</b> copies a plain-text summary. Analysis is optional and read-only: if you never run it, u64deck uses the normal built-in Disk Swap matcher only. Opening the analyser does not save or change anything.</p><p>A high-confidence candidate becomes active only after preview and confirmation. Approve it globally or for the shown folder. Ambiguous candidates can be approved individually, by selected displayed examples, with <b>Approve all ambiguous</b>, or with <b>Approve folder</b>. Approve all covers every current ambiguous set, not only the representative examples, and uses an additional confirmation for large collections. These actions create exact-set overrides only and never create a reusable pattern. Large exact-set lists are paged 50 at a time; selected enable/disable/remove actions apply to the current page.</p><p><b>Remove all local approvals</b> removes every user-approved reusable rule and exact set after two confirmations. Files, index entries and built-in grouping remain unchanged. Local approvals otherwise persist across index rebuilds and apply automatically even when the analyser is not opened. GoodTools square-bracket alternatives, matching unsuffixed siblings and sequel-like glued numbers remain protected from reusable automatic rules.</p>`},
 {id:"local-index",title:"Local USB Indexing",body:`
  <p>For a large USB collection, remove the stick safely from the Ultimate, connect it to this PC, select its Windows path, map it to the matching Ultimate path such as <code>/USB0</code>, and choose <b>Build Index From Local USB</b>.</p>
  <p>The scan is read-only: files are not copied, renamed or modified. It imports directory, image and SID catalogue data directly into the same stable SQLite index used by network browsing. The local path and Ultimate path must describe the same subtree; a wrong mapping creates search paths that the device cannot later open.</p>
  <p>After returning the stick to the Ultimate, normal browsing refreshes individual folders. A full <b>Verify From Ultimate</b> walk is optional and is mainly useful after the collection changed while disconnected.</p>`},
 {id:"jukebox",title:"SID Jukebox",body:`
  <p>Browse SID folders, use Search All of HVSC, explicitly load a folder as the Play Queue, or choose <b>Random Dive</b>. Selecting an individual search or browser result now creates a one-tune queue and starts it immediately; it does not silently add every SID in the composer's folder. Use <b>＋</b> to add only that tune, or <b>♫ Play This Folder</b> when a complete folder queue is what you want. Use the star beside the playback controls to favourite the exact tune currently playing without finding its row in the queue.</p>
  <p>⏮ / ⏭ step through the queue. <b>Stop</b> resets to the normal C64 screen because the Ultimate firmware exposes no separate SID-player stop operation. When Ethernet is selected with verified Wi-Fi REST control, u64deck temporarily parks any configured fast cartridge, resets through the responsive Wi-Fi REST path, then restores the cartridge setting without activating it. This avoids the delayed wired command path and prevents Stop from booting the cartridge. Single-interface Legacy/C64U and CIA1-capable U64 sessions retain their previously verified REST/command-socket order and compatibility fallback, using the cached capability result without adding a network probe to the Stop click.</p>
  <p><b>Clear Queue</b> turns Radio off, resets the current SIDFlow session and removes the tunes waiting behind the current SID. A SID already playing is not abruptly silenced: it continues to its documented end, where u64deck stops instead of restarting it or advancing to another tune. When nothing is playing, Clear Queue empties the queue completely. Saved play queues, favourites and downloaded similarity data are never deleted.</p>
  <p>Starting other software takes ownership of the C64. Mount &amp; Run, Mount &amp; Load, PRG/cartridge runners and explicit Reset or Reboot cancel pending SID completion/auto-advance callbacks and turn Radio off before proceeding. The queue is retained, but no delayed Jukebox action can reset the machine or start another SID after the new program begins.</p>
  <p>Some Ultimate firmware/device combinations retain native SID-player state after playback has stopped or finished, even though the C64 has returned to READY. A normal Reset does not always clear that state. u64deck therefore remembers successful SID-runner use for each connected device. Before the next <b>Mount &amp; Run</b>, it performs one full Reboot, waits for the Ultimate to return, then mounts the image and continues through the normal reset, readiness gates, LOAD and RUN sequence. Mount &amp; Run remains unchanged when no SID has played, and using the explicit Reboot control clears the pending recovery requirement.</p>
  <p>Random Dive first searches the SQLite storage catalogue using normalised, case-insensitive paths. For an HVSC folder it can fall back to the Songlengths path index after mapping the configured HVSC root.</p>
  <p>When Songlengths is loaded, u64deck generates the compact per-SID .ssl duration data expected by the Ultimate and sends that as the optional second attachment with uploaded SID files. This lets the native SID-player screen show the documented duration for the selected subtune instead of its generic fallback length. The complete Songlengths database is never uploaded, and playback still works normally when no matching duration is available. Jukebox auto-advance waits a short post-length grace (0.5 seconds by default, configurable as <code>sid_jukebox_end_grace_secs</code>) when streamed fade is disabled. The optional <b>Fade streamed SID ending</b> control starts a browser-side fade at the documented endpoint and keeps the selected native subtune alive for the chosen 1–5 second fade. While enabled, the fade duration replaces the separate end grace. At zero volume the browser remains silent until a new playback generation is confirmed, clears the previous SID's buffered tail, then restores full gain for the replacement tune. It affects audio heard through this browser and browser recordings only. Ultimate HDMI and analogue output remain at full volume until the extended native endpoint, so the option is labelled directly in the Jukebox UI.</p>
  <h3>SIDFlow recommendations and Radio</h3><p><b>♪ More like this</b> beside Now Playing uses the tune currently being heard. The smaller ♪ action on a Play Queue row uses that row as the similarity seed, then inserts the matches after the current tune. SIDFlow 0.8.0 supplies a weighted 58-dimensional neighbour graph for HVSC 85. u64deck filters it to tunes present in the current SID index, prefers different SID files before sibling subtunes, preserves the selected subsong and inserts up to 20 results immediately after the current tune. Presence uses the union of ordinary indexed .sid files and optional SID metadata, so a partial metadata refresh or a single played-SID metadata row cannot hide the rest of the collection. Adding recommendations or individual SIDs edits the future queue without invalidating the current tune's auto-advance timer.</p>
  <p>Ticking <b>Radio</b> does not replace the current queue. Playback continues normally; when only a few entries remain, u64deck uses the most recently played SID/subsong as the next seed and appends similar indexed HVSC tunes. A session-level played set prevents repeats. Turning Radio off stops future top-ups but leaves tunes already added in place. Clear Queue always disarms Radio first, so an intentionally cleared queue cannot refill itself.</p>
  <p>If SIDFlow 0.8.0 is absent or an older data release is installed, More like this and Radio offer to update it. Older data is shown as <b>Update required</b>, remains on disk, and is deliberately not used for recommendations. With no network available the download error is shown, the old database remains untouched but gated, and normal SID browsing, queueing and playback continue. Settings → Search Index &amp; Cache shows the pinned release, free-space preflight, compressed download, checksum verification, decompression, neighbour import and atomic promotion stages. Local uploads, non-HVSC tunes and collection-version path drift produce a one-line explanation.</p>
  <p><b>Powered by SIDFlow (Chris Gleissner).</b> <a href="https://github.com/chrisgleissner/sidflow" target="_blank">SIDFlow project</a>. u64deck downloads the pinned <a href="https://github.com/chrisgleissner/sidflow-data/releases/tag/0.8.0" target="_blank">0.8.0 full export</a> as a 194 MB gzip, verifies both compressed and decompressed SHA-256 digests, imports the 2,196,700 weighted neighbour rows and removes the temporary 982 MB source. The resulting hardware-tested database was about 257 MiB; the full real update completed in about 48 seconds on the tested Windows PC, with slower network or storage taking longer. Radio excludes current, recent and queued SID files. If the fixed-depth graph is exhausted, a clearly labelled local 48-dimensional fallback fills remaining slots. Recommendations stay local; u64deck does not upload listening activity, ratings or queue contents.</p>
  <p>Play queues remain lazy: u64deck fetches the complete SID only when it is actually played. The optional <b>SID Index &amp; Metadata</b> scan reads only each small PSID/RSID header and stores title, author, chip, format, subtune count and related fields in SQLite. When Songlengths.md5 is loaded, the <b>Length</b> column is also filled immediately from its HVSC path/subsong catalogue; an unknown duration is shown as —. This pre-populates the Play Queue without bulk-fetching tune data while the Ultimate's native SID player is running.</p>
  <p>The Play Queue expands to use the remaining Jukebox height and keeps its column headings visible while the rows scroll. The playing row has a persistent ▶ marker, stronger highlight and left accent that remain distinct from the lighter hover state. When playback starts or advances, the queue reveals that row once without continually overriding manual scrolling; use <b>◎ Current</b> to jump back to it at any time. For a composer folder where every tune has the same indexed author, the author is shown once in the queue heading instead of being repeated on every row. Mixed-author queues retain the Author column; narrower windows fold author and release details beneath the title.</p>
  <p><b>Saved play queues before playback:</b> the selector and Load button remain visible whenever the active queue is empty — on first startup or a direct Jukebox visit, after Clear Queue, and before choosing a tune through Search, folder browsing or local upload. The same empty state is used while Storage, Favourites or Recent Items entry is waiting to populate. Save and Clear Queue require an active queue; Delete requires a selected saved queue.</p>
  <h3>SID metadata refresh</h3><p>Use the clearly labelled <b>SID Index</b> button in the Jukebox browser toolbar to open or close the indexing controls. After a completed scan the button shows the total indexed tune count, for example <b>SID Index · 61,157</b>; during a scan it shows live parsed progress. <b>Refresh From Ultimate</b> reads SID headers over FTP. <b>Build From Local HVSC</b> is considerably faster for a complete collection: connect the drive to this PC, select the local HVSC folder (the one containing MUSICIANS and DOCUMENTS), and map it to its normal Ultimate path such as <code>/USB0/HVSC</code>. Both scans are incremental, read-only, pausable and stoppable. Leave <b>rescan unchanged files</b> off for normal refreshes; enable it only when deliberately rebuilding all stored header metadata. A completed local index does not need to be rebuilt unless the collection changes or you intentionally request a full rescan.</p>
  <h3>Search filters and chip badges</h3><p><b>Chip</b> filters 6581, 8580, Either, Mixed/Multi-SID or Unknown declarations. Badge colours identify metadata consistently in search results, Play Queue and Now Playing: amber 6581, cyan 8580, violet Either, pink Mixed/Multi-SID and grey Unknown. The selected value in the <b>Chip</b> dropdown — not the badge colour — indicates the active filter. <b>Format</b> filters PSID or RSID. <b>Year</b> accepts an exact four-digit year from 1900 to 2099 and matches a standalone year in the SID release metadata. A text term is optional, so any metadata filter can be used by itself or combined with the others. These filters require the SID metadata index; ordinary filename/path search still falls back to Songlengths on an older index.</p>
  <p>The live audio badge includes browser audio chunks per second. Around 31/s is normal for the Ultimate's approximately 48 kHz stream.</p>`},
 {id:"assembly",title:"Assembly64",body:`
  <p><b>Search Assembly64</b> queries the configured public service using the same AQL-style fields as the Ultimate firmware. Enter a title, group, handle or other supported term and optionally narrow by category, minimum rating or recency. Results remain in the left pane while the selected release and its downloadable files appear on the right.</p>
  <p>PRGs and cartridges can run directly. Supported disk images can be inspected, mounted to either drive or started with <b>Mount &amp; Run</b>; all mount actions use the current Default Mount Mode. For a confidently named multi-disk family in the same release, u64deck downloads only the related images and arms Disk Swap before the selected disk is mounted, including the first mount after a clean start. Ambiguous and single-disk releases remain one-disk sets. Opening a release adds it to Recently Used, and the star saves the release itself in Favourites so its file list can be reopened later.</p>
  <p>Downloads are bounded and happen only when an action is selected. <b>Developer Details</b> remains collapsed unless raw service fields are needed for troubleshooting a changed response or custom endpoint template.</p>`},
 {id:"health",title:"System Health",body:`
  <p>The <b>HEALTH</b> tab combines cached Ultimate status with local u64deck, browser, stream, queue, index, cache and lifecycle telemetry. It refreshes automatically every two seconds only while the tab is visible and stops when the tab or browser page is hidden. The timestamp confirms the automatic update, so no manual Refresh control is required. A top summary uses green, amber or red only for current or recent unresolved conditions; recovered and historical warnings remain inspectable without leaving the dashboard permanently degraded.</p>
  <h3>No extra Ultimate polling</h3><p>The dashboard does not call the Ultimate. REST identity, success/failure counts, p95 latency and failure streaks come from the normal status request already made by u64deck approximately every 30 seconds. Process, UDP stream, queue, browser and SQLite catalogue figures are read locally; index totals are cached for 15 seconds.</p>
  <h3>Stream and browser health</h3><p>Video and audio bitrate, received/rendered FPS, packets per second, dropped packets, gap events, longest gaps, packet age, stream restarts and WebSocket lifecycle are shown together. The browser also reports Web Audio queue depth and underruns to the local backend. <b>Waiting</b> means a stream was requested but packets have not arrived recently.</p>
  <h3>Queue, task, index and cache health</h3><p>The dashboard shows the current operation and phase, queue wait distribution and recent device operations. Index cards show the current path, throughput, ETA and changes. Cache figures cover image-cache occupancy, hit rate, evictions and lazy SID materialisation. Detailed histories stay collapsed until opened.</p>
  <h3>Hardware telemetry limits</h3><p>The current Ultimate REST API does not expose CPU utilisation, FPGA utilisation or temperature. u64deck therefore reports these values as unavailable rather than estimating them. The FPGA and core <i>version</i> fields remain visible from the normal information response.</p>
  <p>The same snapshot and bounded in-memory histories are included in <b>Export Diagnostics</b>. No telemetry database is kept.</p>`},
 {id:"settings",title:"Settings",body:`
  <h3>Desktop App</h3><p>This controls what <code>start.bat</code> opens on this PC. Edge app mode uses a dedicated u64deck window and profile without changing the Windows default browser. The choice applies the next time the launcher starts.</p>
  <h3>Ultimate configuration</h3><p>Categories and values are read from the connected firmware. On startup or a hard browser refresh, the Settings page waits for the normal Ultimate information check, shows a neutral retrying state and loads automatically when the connection is ready. A transient refused connection is not presented as a settings failure; only a bounded persistent failure is shown, with a manual retry control. <b>Apply Changes</b> changes the running configuration only. <b>Save to Flash</b> makes the current device settings persistent, while <b>Load from Flash</b> restores the saved set. Factory Defaults resets all Ultimate settings and should be used carefully. These controls affect the device; they are separate from u64deck's local <code>config.json</code>.</p>
  <h3>Desktop app</h3><p><b>Exit u64deck</b> stops the local Python server, runs the normal stream, input, index and database cleanup, and closes the dedicated Edge app window when u64deck launched it. The Ultimate itself keeps running. For safety, exit can be requested only from the UI opened on the same PC.</p>
  <h3>Search Index &amp; Cache</h3><p>This panel shows the active stable database filename, migration result, catalogue counts, job status and image parse failures. Clearing the image cache or storage index removes only local catalogue data and never deletes content from the Ultimate. <b>Export Diagnostics</b> creates a sanitised support bundle.</p>
  <h3>Local USB and SID metadata</h3><p>Local USB indexing imports a large collection without FTP traffic. SID metadata scans read only PSID/RSID headers and optionally Songlengths data; they do not bulk-load tune bodies into memory.</p>
  <h3>SIDFlow Similarity Data</h3><p>This installs the pinned <a href="https://github.com/chrisgleissner/sidflow-data/releases/tag/0.8.0" target="_blank">SIDFlow 0.8.0 full export</a> for HVSC 85. The 194 MB compressed asset expands temporarily to about 982 MB, so allow roughly 1.8 GiB free. The hardware-tested compact database was about 257 MiB and the complete real update took about 48 seconds on a fast Windows PC. u64deck validates schema <code>sidcorr-1</code>, weighted-cosine metadata, 58 vector weights, track/neighbour counts and pinned compressed/uncompressed SHA-256 digests before atomically promoting the compact database. An older working database is preserved on any failure but is marked Update required and remains gated until 0.8.0 is installed; without network, recommendations remain unavailable while normal SID playback continues. Remove deletes only local similarity data.</p>
  <p>Similarity recommendations are <b>Powered by SIDFlow (Chris Gleissner)</b>. <a href="https://github.com/chrisgleissner/sidflow" target="_blank">Project page</a>.</p>`},
 {id:"diagnostics",title:"Diagnostics & Troubleshooting",body:`
  <p><b>Export Diagnostics</b> creates a sanitised ZIP containing release/build details, System Health, connectivity, stream counters, queue and index statistics, parse failures and recent warnings. In supported browsers the chosen file is explicitly written and closed before success is reported; other browsers use the normal Downloads fallback. Passwords and content files are excluded.</p>
  <h3>Controls stop responding during SID playback</h3><p>Browser requests are bounded, Jukebox polling cannot overlap and queue entries are loaded lazily. Stop also clears audio already scheduled by Web Audio, so buffered sound does not continue after the reset request. Busy or unavailable device operations time out without freezing the interface.</p>
  <h3>Random Dive reports no result</h3><p>The message distinguishes an unindexed folder, a folder containing no SID files, and an HVSC path-mapping problem. Re-detect HVSC only for the mapping case.</p>
  <h3>Mount &amp; Run shows BUSY</h3><p>This is expected during a slow genuine-drive load. The Ultimate's small HTTP service can be occupied while the emulated drive is transferring data. u64deck keeps the connection and confirmed Mounted Drives state, withholds RUN until the readiness gate succeeds, and refreshes status when loading completes.</p>
  <h3>Device finder does not show an old address</h3><p>The finder intentionally lists only addresses verified in the current scan. Remembered addresses are prioritised inside the same concurrent pass as the local subnet, but stale entries are never displayed from history alone. Check that the interface is enabled, on the same local subnet and has Web Remote Control enabled, then scan again.</p>
  <h3>Discovery is running</h3><p>Normal status and Mounted Drives polling pause as soon as Finder opens and remain paused during the bounded subnet scan. This is deliberate REST etiquette for the Ultimate's embedded HTTP service. Polling resumes automatically when Finder completes; repeated scans and immediate retry storms are avoided.</p>
  <h3>Blank image times out</h3><p>Refresh the folder before retrying because the firmware may have completed creation after the response timed out.</p>
  <h3>Image not parsed</h3><p>Copy-protected, truncated or non-standard images can have invalid directory chains. The original is not modified and can still be mounted.</p>`}
];
